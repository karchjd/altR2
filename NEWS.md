# altR2 1.1.1

* Fixed failures for perfectly fitted models and `lm` objects created with
  `model = FALSE`.
* Added validation of the arguments to `estimate_adj_R2()`.

# altR2 1.1.0

* Added `estimate_adj_R2` such that the package can also be called when R-squared is not directly estimated from `lm`
